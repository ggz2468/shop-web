import { describe, it, expect } from 'vitest';

describe('數學運算', () => {
  it('1 + 1 應等於 2', () => {
    expect(1 + 1).toBe(2);
  });

  it('2 * 3 應等於 6', () => {
    expect(2 * 3).toBe(6);
  });
});
